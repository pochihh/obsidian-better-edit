# Tests

This folder is reserved for automated tests.

Recommended first targets:

- block-model parsing helpers
- text styling wrapper normalization
- slash command explicit trigger state
- symbol picker shortcut matching

Fixture-driven manual editor tests live separately under [`test-vault/`](../test-vault/).
